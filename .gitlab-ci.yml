stages:
        - build
        - test
        - deploy

Compile:
        when: always
        image: ubuntu:latest
        stage: build
        script:
                - echo "Helo i am build"
Style:
        when: always
        stage: test
        script:
                - echo "Hello test"
Prodaction:
        when: always
        stage: deploy
        script:
                - echo "Hello ddeploy"
