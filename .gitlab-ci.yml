stages:
        - build
        - test
        - deploy

Compile:
        when: always
        image: ubuntu:latest
        stage: build
        script:
                - make -C src/SimpleBash/cat s21_cat
                - make -C src/SimpleBash/grep s21_grep
        artifacts:
                paths:
                        - src/cat/s21_cat
                        - src/grep/s21_grep
                expire_in: 30 days

Style:
        when: always
        stage: test
        script:
                - make -C src/SimpleBash/cat lint
Prodaction:
        when: always
        stage: deploy
        script:
                - echo "Hello ddeploy"
