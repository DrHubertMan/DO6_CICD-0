defalut:
        image: ubuntu:latest
stages:
        - build
        - test
        - deploy

Compile:
        when: always
        stage: build
        script:
                - echo "Hello i am build"
Style:
        when: always
        stage: test
        script:
                - echo "Hello test"
Prodaction:
        when: always
        stage: deploy
        script:
                - echo "Hello deploy"
